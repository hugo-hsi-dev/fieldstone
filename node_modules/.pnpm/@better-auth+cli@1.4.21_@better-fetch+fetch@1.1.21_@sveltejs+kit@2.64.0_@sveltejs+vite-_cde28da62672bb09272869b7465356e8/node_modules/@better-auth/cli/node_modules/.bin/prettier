#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../prettier/bin/prettier.cjs" "$@"
else
  exec node  "$basedir/../../../../prettier/bin/prettier.cjs" "$@"
fi
# cmd-shim-target=/Users/hugo-hsi/Projects/fieldstone/node_modules/.pnpm/@better-auth+cli@1.4.21_@better-fetch+fetch@1.1.21_@sveltejs+kit@2.64.0_@sveltejs+vite-_cde28da62672bb09272869b7465356e8/node_modules/prettier/bin/prettier.cjs
