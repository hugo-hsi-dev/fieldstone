#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../vitest/vitest.mjs" "$@"
else
  exec node  "$basedir/../../../vitest/vitest.mjs" "$@"
fi
# cmd-shim-target=/Users/hugo-hsi/Projects/fieldstone/node_modules/.pnpm/better-auth@1.4.21_@prisma+client@5.22.0_@sveltejs+kit@2.64.0_@sveltejs+vite-plugin-sve_5294b78002358fdca7ca853ac745c0df/node_modules/vitest/vitest.mjs
