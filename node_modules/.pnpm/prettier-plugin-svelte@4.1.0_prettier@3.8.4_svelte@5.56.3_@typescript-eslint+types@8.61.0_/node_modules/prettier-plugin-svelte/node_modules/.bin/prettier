#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../prettier/bin/prettier.cjs" "$@"
else
  exec node  "$basedir/../../../prettier/bin/prettier.cjs" "$@"
fi
# cmd-shim-target=/Users/hugo-hsi/Projects/fieldstone/node_modules/.pnpm/prettier-plugin-svelte@4.1.0_prettier@3.8.4_svelte@5.56.3_@typescript-eslint+types@8.61.0_/node_modules/prettier/bin/prettier.cjs
