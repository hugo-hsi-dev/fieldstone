#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

exec "$basedir/../../../esbuild/bin/esbuild"   "$@"
exit $?
# cmd-shim-target=/Users/hugo-hsi/Projects/fieldstone/node_modules/.pnpm/vite@8.0.16_@types+node@24.13.1_esbuild@0.28.0_jiti@2.7.0_tsx@4.22.4/node_modules/esbuild/bin/esbuild
