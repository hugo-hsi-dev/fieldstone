#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../typescript/bin/tsc" "$@"
else
  exec node  "$basedir/../../../../typescript/bin/tsc" "$@"
fi
# cmd-shim-target=/Users/hugo-hsi/Projects/fieldstone/node_modules/.pnpm/@typescript-eslint+eslint-plugin@8.61.0_@typescript-eslint+parser@8.61.0_eslint@10.4.1__488aaaa836250ff05df2fe2b5ef67f6b/node_modules/typescript/bin/tsc
