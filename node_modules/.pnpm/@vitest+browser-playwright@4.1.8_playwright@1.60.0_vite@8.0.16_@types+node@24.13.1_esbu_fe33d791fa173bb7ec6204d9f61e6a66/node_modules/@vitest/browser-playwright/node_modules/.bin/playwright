#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../playwright/cli.js" "$@"
else
  exec node  "$basedir/../../../../playwright/cli.js" "$@"
fi
# cmd-shim-target=/Users/hugo-hsi/Projects/fieldstone/node_modules/.pnpm/@vitest+browser-playwright@4.1.8_playwright@1.60.0_vite@8.0.16_@types+node@24.13.1_esbu_fe33d791fa173bb7ec6204d9f61e6a66/node_modules/playwright/cli.js
